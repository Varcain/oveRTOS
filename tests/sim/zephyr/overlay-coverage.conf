# Coverage overlay for Zephyr native_sim — applied via EXTRA_CONF_FILE by
# `ove test zephyr-coverage`. Zephyr writes .gcda files into the build tree
# at native_sim exit, ready for the standard lcov capture path.
CONFIG_COVERAGE=y
CONFIG_COVERAGE_GCOV=y
