# Auto-generated by board_tool.py -- BEGIN
CONFIG_I2S=y
CONFIG_I2C=y
CONFIG_DMA=y
CONFIG_AUDIO=y
CONFIG_AUDIO_CODEC=y
CONFIG_DISK_ACCESS=y
CONFIG_DISK_DRIVERS=y
CONFIG_DISK_DRIVER_SDMMC=y
# Keep each DMA transfer to one 512-byte sector in an owned staging buffer.
# This avoids the STM32F7 HAL multiblock stop-command state machine without
# exposing caller cache lines to DMA.
CONFIG_SDMMC_STM32_SINGLE_BLOCK=y
CONFIG_DISPLAY=y
CONFIG_HEAP_MEM_POOL_SIZE=20480
# Auto-generated by board_tool.py -- END

# Copyright (C) 2026 Kamil Lulko <kamil.lulko@gmail.com>
#
# SPDX-License-Identifier: GPL-3.0-or-later
#
# This file is part of oveRTOS.

CONFIG_I2S=y
CONFIG_I2C=y
# CONFIG_USERSPACE shifts final device objects by eight bytes after Zephyr has
# generated its static ISR arguments (the Ethernet driver has the same board
# workaround in patches/). Use the STM32 driver's supported polling path so an
# I2C event cannot dereference the stale device pointer.
CONFIG_I2C_STM32_INTERRUPT=n
CONFIG_DMA=y
CONFIG_AUDIO=y
CONFIG_AUDIO_CODEC=y

CONFIG_HEAP_MEM_POOL_SIZE=65536
CONFIG_MAIN_STACK_SIZE=8192

CONFIG_LOG=y
CONFIG_I2S_LOG_LEVEL_DBG=y
CONFIG_SDMMC_LOG_LEVEL_DBG=y
CONFIG_DISK_LOG_LEVEL_DBG=y
