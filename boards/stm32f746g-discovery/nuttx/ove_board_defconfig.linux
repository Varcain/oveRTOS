# NuttX defconfig overlay for the Linux personality on stm32f746g-discovery (real hardware).
# Applied automatically when CONFIG_OVE_LINUX=y (layer 2b, see
# config/ove-cli/ove/rtos_menuconfig.py:_merge_nuttx_layers).
#
# The personality loads FDPIC programs and runs them UNPRIVILEGED behind an MPU view the seam
# programs DIRECTLY via raw registers (backends/nuttx/nuttx_lnx_trap.c: lxp_mpu_init + PRIVDEFENA
# + a MemManage containment handler) — done in plain CONFIG_BUILD_FLAT, no CONFIG_BUILD_PROTECTED. We
# own the MPU, so NuttX's own arm_mpu.c must stay OUT of the build. It is already off by default on
# this board; pin it off so a base-config change can't silently re-enable it and fight the seam.
CONFIG_ARM_MPU=n

# Use the M7 D-cache in its normal write-back mode. The seam now gives the
# privileged coordinator base and each guest overlay identical WBWA,
# non-shareable Normal-memory attributes; it no longer depends on FORCEWT to
# mask a mixed cacheable/uncached CPU alias. Region reuse and copied executable
# publication are explicit cache-maintenance boundaries. Ethernet already owns
# its DMA clean/invalidate transitions, while the LTDC framebuffer is excluded
# from the cacheable base region by an MPU subregion-disable bit.
CONFIG_ARMV7M_DCACHE_WRITETHROUGH=n

# The personality's svc handler runs on NuttX's interrupt stack and calls into the syscall core + the
# app's I/O callbacks (including the polled USART1 console); give it headroom over the 2 KB default.
CONFIG_ARCH_INTERRUPTSTACK=4096

# Park blocked Linux guests in NuttX's stopped-task list and return the same TCB
# to the ready list on completion. Without this option the scheduler has no
# persistent STOPPED state and the personality would have to delete/recreate
# tasks on every wait.
CONFIG_SIG_DEFAULT=y
CONFIG_SIG_SIGSTOP_ACTION=y

# Physical FT5336 touch controller on I2C3 (PH7=SCL, PH8=SDA). board_init.c
# registers this controller as /dev/i2c2, matching ove's zero-based instance 2.
CONFIG_I2C=y
CONFIG_I2C_DRIVER=y
CONFIG_STM32F7_I2C3=y
# The only personality I2C client is the 60 Hz FT5336 poller.  Service its
# short transfers from the preemptible coordinator thread instead of creating
# event-IRQ chains which can tail-chain ahead of the just-woken critical RT
# thread.  Bound a wedged controller to the 25 ms timeout requested by the
# portable touch driver rather than NuttX's 500 ms defaults.
CONFIG_I2C_POLLED=y
CONFIG_STM32F7_I2CTIMEOMS=25
CONFIG_STM32F7_I2CTIMEOTICKS=25

# Fold the 64 KB DTCM into the heap (region 3). NuttX's SRAM-only heap here is tiny (SRAM1 tail +
# SRAM2 ~= 31 KB) — barely enough for the 16 KB CONFIG_INIT_STACKSIZE init task, so any extra .bss
# (the personality's socket-storage pool + net-stack buffers) pushes the init-task stack allocation
# past the largest free block and the app never spawns (silent: only Idle + hpwork exist). DTCM is
# otherwise unused and DMA never reaches it (the ETH/IOB buffers are static in SRAM, the guest pools
# are in SDRAM), so it is safe as general heap. Config 3 in stm32_allocateheap.c = SRAM1+SRAM2+DTCM.
# CONFIG_STM32F7_DTCMEXCLUDE is not set
CONFIG_MM_REGIONS=3

# The NuttX console backend reads RX through NuttX's IRQ-filled serial buffer and puts its
# non-blocking descriptor in raw mode so NuttX does not also echo each keystroke (the guest shell
# owns echo) or line-buffer to a newline. tcsetattr/TCSETS need termios.
CONFIG_SERIAL_TERMIOS=y

# Inter-program isolation: a note driver (backends/nuttx/nuttx_lnx_trap.c) hooks EVERY
# context switch — including round-robin preemption between same-priority program tasks, which the
# seam's syscall path cannot see — to reprogram the MPU to the incoming program's OWN region. That
# hook is the sched_note switch instrumentation, dispatched through the note-driver framework
# (CONFIG_DRIVERS_NOTE compiles note_driver.c = sched_note_* + note_driver_register). We register
# our own driver only; no note buffer (NOTERAM/etc.) is enabled.
#
# On the real M7 the per-switch reprogram narrows the MPU to only the running program's pool region;
# every OTHER program's region then falls to the ARM default map, which types the external SDRAM at
# 0xC0000000 as DEVICE. lxp_mpu_init therefore keeps a privileged-only whole-pool base region so
# the coordinator still sees all pool memory as Normal (an unaligned access to Device memory
# Usage-Faults on silicon — QEMU's an500 PSRAM default is Normal, so that build never hit it).
CONFIG_DRIVERS_NOTE=y
CONFIG_DRIVERS_NOTERAM=n
CONFIG_SCHED_INSTRUMENTATION=y
CONFIG_SCHED_INSTRUMENTATION_SWITCH=y

# QUADSPI controller for the QSPI-XIP rootfs (CONFIG_OVE_LINUX_ROOTFS_QSPI). board_init.c uses
# stm32f7_qspi_initialize + stm32f7_qspi_enter_memorymapped to memory-map the N25Q128A NOR at
# 0x90000000 (read-only, 0xEB quad). Do NOT enable CONFIG_MTD_N25QXXX — its board setup mounts
# NXFFS on the NOR, which would try to format/erase the already-programmed rootfs. (Bank 1 +
# 16 MB flash size are the Kconfig defaults.)
CONFIG_STM32F7_QUADSPI=y
CONFIG_STM32F7_QSPI_POLLING=y
CONFIG_STM32F7_QSPI_FLASH_SIZE=16777216
# Chip-select-high time between QSPI transactions.  The driver default is 1 cycle (DCR.CSHT=0);
# the STM32Cube/FreeRTOS BSP uses 6 (QSPI_CS_HIGH_TIME_6_CYCLE).  At the 108 MHz XIP read clock 1
# cycle (~9 ns) is too little N25Q deselect margin — verified on silicon by diffing the live
# QUADSPI registers vs the working FreeRTOS build: CSHT was the sole DCR difference, and its
# CS-toggle sensitivity is exactly why single (uncached) reads corrupt worse than cached bursts.
CONFIG_STM32F7_QSPI_CSHT=6

# Guest entropy: back /dev/urandom with the STM32 hardware TRNG so the
# personality's AT_RANDOM seed and guest getrandom(2) draw real entropy.
# nuttx_random_fill uses non-blocking reads and fails closed on any unavailable
# or unhealthy source. STM32F7_RNG registers both random devices; the RNG clock
# is PLL_VCO/PLLQ, well above HCLK/16, so there is no clock-error condition.
CONFIG_STM32F7_RNG=y
CONFIG_DEV_RANDOM=y
CONFIG_DEV_URANDOM=y
CONFIG_DEV_URANDOM_ARCH=y
