# Copyright (C) 2026 Kamil Lulko <kamil.lulko@gmail.com>
#
# SPDX-License-Identifier: GPL-3.0-or-later
#
# This file is part of oveRTOS.
#
# Base Zephyr prj.conf for the QEMU MPS2-AN521 board. Zephyr requires a prj.conf
# in the application (board) folder; the real, workspace-specific configuration
# (USERSPACE, the personality knobs, modules, etc.) is generated by `ove
# configure` and layered on top via -DEXTRA_CONF_FILE=<generated>/merged_prj.conf.
